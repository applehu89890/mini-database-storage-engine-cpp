# mini-database-storage-engine-cpp
A C++ database systems project implementing core storage engine components, including disk and memory management, page-based storage, record handling, bucket structures, and relational join operations.

This project is a simplified database storage engine implemented in C++. It simulates several core components of a relational database system, including disk and memory management, page-based storage, record handling, bucket structures, and join operations.
